{
  "git": {
    "commitMessage": "Release idle-agent ${version}",
    "tagName": "idle-agent-v${version}",
    "push": true,
    "requireCleanWorkingDir": true,
    "requireBranch": "main",
    "addUntrackedFiles": false
  },
  "github": {
    "release": true,
    "releaseName": "idle-agent-v${version}",
    "releaseNotes": "node .release-it.notes.js ${version}"
  },
  "npm": {
    "publish": true
  },
  "hooks": {}
}
