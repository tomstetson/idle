apiVersion: apps/v1
kind: Deployment
metadata:
  name: idle-server
spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  replicas: 1
  selector:
    matchLabels:
      app: idle-server
  template:
    metadata:
      labels:
        app: idle-server
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9090"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: idle
        image: ghcr.io/tomstetson/idle-server:{version}
        ports:
        - containerPort: 3005
        env:
        - name: NODE_ENV
          value: production
        - name: PORT
          value: "3005"
        - name: REDIS_URL
          value: redis://idle-redis:6379
        - name: S3_PUBLIC_URL
          value: https://files.idle.northglass.io
        envFrom:
        - secretRef:
            name: idle-secrets
        livenessProbe:
          httpGet:
            path: /health
            port: 3005
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 3005
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: idle-server-pdb
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: idle-server
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: idle-secrets
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: SecretStore
  target:
    name: idle-secrets
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: /idle-db
    - extract:
        key: /idle-master
    - extract:
        key: /idle-github
    - extract:
        key: /idle-files
    - extract:
        key: /idle-e2b
    - extract:
        key: /idle-revenuecat
    - extract:
        key: /idle-elevenlabs
---
apiVersion: v1
kind: Service
metadata:
  name: idle-server
spec:
  selector:
    app: idle-server
  ports:
  - port: 3000
    targetPort: 3005
  type: ClusterIP